{% extends 'base.html.twig' %}

{% block title %}PropertyStatus{% endblock %}

{% block body %}
    <h1>PropertyStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_status.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ property_status.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_property_status_index') }}">back to list</a>

    <a href="{{ path('app_property_status_edit', {'id': property_status.id}) }}">edit</a>

    {{ include('property_status/_delete_form.html.twig') }}
{% endblock %}
