<form method="post" action="{{ path('app_studies_currently_delete', {'id': studies_currently.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ studies_currently.id) }}">
    <button class="btn">Delete</button>
</form>
