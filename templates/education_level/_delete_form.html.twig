<form method="post" action="{{ path('app_education_level_delete', {'id': education_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ education_level.id) }}">
    <button class="btn">Delete</button>
</form>
