{% extends 'base.html.twig' %}

{% block title %}EducationLevelType{% endblock %}

{% block body %}
    <h1>EducationLevelType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education_level_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ education_level_type.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ education_level_type.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_education_level_type_index') }}">back to list</a>

    <a href="{{ path('app_education_level_type_edit', {'id': education_level_type.id}) }}">edit</a>

    {{ include('education_level_type/_delete_form.html.twig') }}
{% endblock %}
