{% extends 'base.html.twig' %}

{% block title %}AccountType{% endblock %}

{% block body %}
    <h1>AccountType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ account_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ account_type.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ account_type.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_account_type_index') }}">back to list</a>

    <a href="{{ path('app_account_type_edit', {'id': account_type.id}) }}">edit</a>

    {{ include('account_type/_delete_form.html.twig') }}
{% endblock %}
