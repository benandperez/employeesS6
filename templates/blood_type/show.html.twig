{% extends 'base.html.twig' %}

{% block title %}BloodType{% endblock %}

{% block body %}
    <h1>BloodType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blood_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blood_type.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ blood_type.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blood_type_index') }}">back to list</a>

    <a href="{{ path('app_blood_type_edit', {'id': blood_type.id}) }}">edit</a>

    {{ include('blood_type/_delete_form.html.twig') }}
{% endblock %}
