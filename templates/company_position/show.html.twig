{% extends 'base.html.twig' %}

{% block title %}CompanyPosition{% endblock %}

{% block body %}
    <h1>CompanyPosition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company_position.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company_position.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ company_position.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_company_position_index') }}">back to list</a>

    <a href="{{ path('app_company_position_edit', {'id': company_position.id}) }}">edit</a>

    {{ include('company_position/_delete_form.html.twig') }}
{% endblock %}
