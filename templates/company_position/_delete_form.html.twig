<form method="post" action="{{ path('app_company_position_delete', {'id': company_position.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_position.id) }}">
    <button class="btn">Delete</button>
</form>
