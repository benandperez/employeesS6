{% extends 'base.html.twig' %}

{% block title %}MaritalStatus{% endblock %}

{% block body %}
    <h1>MaritalStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marital_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ marital_status.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ marital_status.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marital_status_index') }}">back to list</a>

    <a href="{{ path('app_marital_status_edit', {'id': marital_status.id}) }}">edit</a>

    {{ include('marital_status/_delete_form.html.twig') }}
{% endblock %}
