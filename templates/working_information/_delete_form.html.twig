<form method="post" action="{{ path('app_working_information_delete', {'id': working_information.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ working_information.id) }}">
    <button class="btn">Delete</button>
</form>
