{% extends 'base.html.twig' %}

{% block title %}PlaceWork{% endblock %}

{% block body %}
    <h1>PlaceWork</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ place_work.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ place_work.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ place_work.address }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ place_work.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_place_work_index') }}">back to list</a>

    <a href="{{ path('app_place_work_edit', {'id': place_work.id}) }}">edit</a>

    {{ include('place_work/_delete_form.html.twig') }}
{% endblock %}
