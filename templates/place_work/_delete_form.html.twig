<form method="post" action="{{ path('app_place_work_delete', {'id': place_work.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ place_work.id) }}">
    <button class="btn">Delete</button>
</form>
