{% extends 'base.html.twig' %}

{% block title %}CompanyDepartment{% endblock %}

{% block body %}
    <h1>CompanyDepartment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company_department.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company_department.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ company_department.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_company_department_index') }}">back to list</a>

    <a href="{{ path('app_company_department_edit', {'id': company_department.id}) }}">edit</a>

    {{ include('company_department/_delete_form.html.twig') }}
{% endblock %}
